package AbstractInterface;

public interface Transport {

    /*public abstract (no required becuase it is already interface)*/ void getSetGo();

/*
-> multiple interface is allowed (interface is one type of inheritance so we can indirectly multuple inheritance)
ex. class vehicle implements transport,implements petrolUser;
 */

}
