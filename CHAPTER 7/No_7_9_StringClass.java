public class No_7_9_StringClass {

    public static void main(String[] args) {
        /*
        stack class

        ->there are two memories : stack(allocate at function call) and heap(allocate run time)
        ->string fall in between
        -> string is a object , it allocate in heap but in special area is called string pool

        -> strings are immutable i.e value of those dont change

        ->we can not compare string by ==  for this str1.equals(str2)
         */
    }
}

